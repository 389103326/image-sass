export default function ReactQuery() {
  console.log("hello ReactQuery");

  // const {data, isLoading} = useQuery(trpc.hello.queryOptions());

  return (
    // <div>react query {data?.hello || 'no data'}
    // {isLoading && <div>loading</div>}
    // </div>
    <div>trpc</div>
  );
}
